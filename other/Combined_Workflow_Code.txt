def save_metadata_to_blob(metadata_list: List[Dict], save_path: str) -> None:
    """
    Save the enriched metadata to a JSON file in the mounted blob storage.
    
    Args:
        metadata_list (List[Dict]): List of dictionaries containing enriched metadata
        save_path (str): Full path where to save the JSON file in mounted storage
            e.g., '/mnt/blob-storage/metadata/file_metadata.json'
    """
    try:
        # Ensure the directory exists
        os.makedirs(os.path.dirname(save_path), exist_ok=True)
        
        # Convert to JSON string with proper formatting
        json_data = json.dumps(metadata_list, indent=2, default=str)
        
        # Write to file
        with open(save_path, 'w') as f:
            f.write(json_data)
            
        print(f"Successfully saved metadata to {save_path}")
        
    except Exception as e:
        print(f"Error saving metadata to {save_path}: {str(e)}")
        raise
