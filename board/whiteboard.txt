def tag_as_research_plan(file_path: str, min_threshold: int = 4) -> Dict[str, Any]:
    """
    Tags files as research plans based on scoring criteria.
    Returns empty dict if score is below minimum threshold.
    
    Args:
        file_path: Path to the file
        min_threshold: Minimum score required for research plan classification
        
    Returns:
        Dictionary with keyword_tag and score if classified as research plan,
        empty dict otherwise
    """
    score = calculate_research_plan_score(file_path)
    if score >= min_threshold:
        return {'keyword_tag': 'research-plan', 'score': score}
    return {}

def tag_as_research_guide(file_path: str, min_threshold: int = 4) -> Dict[str, Any]:
    """
    Tags files as research guides based on scoring criteria.
    Returns empty dict if score is below minimum threshold.
    
    Args:
        file_path: Path to the file
        min_threshold: Minimum score required for research guide classification
        
    Returns:
        Dictionary with keyword_tag and score if classified as research guide,
        empty dict otherwise
    """
    score = calculate_research_guide_score(file_path)
    if score >= min_threshold:
        return {'keyword_tag': 'research-guide', 'score': score}
    return {}
