query = "SELECT file_relative_path FROM vfs_raw.file_metadata;"

# Execute the query and get the results in a DataFrame
df = execute_query(query)

# Augment the relative paths with the root path
df['file_path'] = root_path + df['file_relative_path']

# Convert the file paths to a list
file_paths_list = df['file_path'].tolist()
